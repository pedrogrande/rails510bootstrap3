<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">

    <%# render 'shared/social_metatags' %>
    <title><%= content_for?(:title) ? content_for(:title) : 'Baxter and Me' %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="//cdn.materialdesignicons.com/1.9.32/css/materialdesignicons.min.css">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style media="screen">
    .parallax-bg-4 {
        background: url(<%= asset_path 'Beach.jpg' %>) center 0 no-repeat;
        background-size: cover;
    }
    .overlay-bg-not-so-dark {
    
    width: 100%;
    height: 100%;
    position: absolute;
    z-index: -1;
    top: 0;
    left: 0;
}
    </style>
  </head>

  <body>
    <div class="wrapper">
      <%= yield %>
      <%= render 'interface/footer' %>
    </div>
    <script type="text/javascript">
      $('.mute').click(function() {
        var vid = document.getElementById("landing-video");
        if (vid.muted == false) {
          vid.muted = true;
        } else {
          vid.muted = false;
        }
      });
    </script>
  </body>
</html>
